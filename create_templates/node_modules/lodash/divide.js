var createMathOperation = require('./_createMathOperation');

/**
 * pide two numbers.
 *
 * @static
 * @memberOf _
 * @since 4.7.0
 * @category Math
 * @param {number} pidend The first number in a pision.
 * @param {number} pisor The second number in a pision.
 * @returns {number} Returns the quotient.
 * @example
 *
 * _.pide(6, 4);
 * // => 1.5
 */
var pide = createMathOperation(function(pidend, pisor) {
  return pidend / pisor;
}, 1);

module.exports = pide;
